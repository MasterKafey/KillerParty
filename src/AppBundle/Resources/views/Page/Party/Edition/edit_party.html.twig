{% extends '@Layout/Default/base.html.twig' %}

{% block content %}

    {{ form_start(form) }}
    {% embed '@Widget/Box/Primary/base.html.twig' %}
        {% block body %}
            {{ form_row(form.name, {'label': 'Nom'}) }}
            {{ form_row(form.cardTypes, {'label': 'Type de cartes'}) }}
            {{ form_rest(form) }}
        {% endblock body %}

        {% block footer %}
            <button type="submit" class="btn btn-primary pull-right">Sauvegarder la partie</button>
        {% endblock footer %}
    {% endembed %}
    {{ form_end(form) }}
{% endblock content %}
